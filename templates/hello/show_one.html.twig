{% extends 'base.html.twig' %}
{% block body %}
<div>The current messages is:</div>
<div>
    <b>
        <div>{{ message.message }}</div>
        {{ include('hello/_message.html.twig', {message: message}) }}
    </b>
</div>

{% endblock %}